# notify.guru Public API

> 実装が Single Source of Truth。この文書は、公開エンドポイントと意図する振る舞いだけを列挙する。パラメータおよびデータ形式は定義しない。

## 共通

### `GET /api/health`

- サービスがリクエストを受け付けられることを返す。

## デバイス

### `POST /api/devices`

- デバイスが署名公開鍵を登録し、サーバはその秘密鍵による署名を検証して一意なdevice IDを発行する。

### `PUT /api/devices/:deviceId/push`

- デバイスが現在のAPNsトークンを登録または更新する。
- device IDに結び付いた秘密鍵による署名を必要とする。
- APNsトークンはデバイスに一つだけ保持し、以前の値を置き換える。

## デバイス参加依頼

### `POST /api/device-requests`

- 新しいデバイスが、既存のデバイスグループへ参加するための短命な依頼を作る。
- 依頼はデバイスグループから独立しており、QRコードまたはURLで既存デバイスへ渡す。
- 依頼を一覧、検索、拒否または延長する操作は設けず、承認されなければ失効する。

### `GET /api/device-requests/:requestId`

- 依頼を作ったデバイスが、依頼の承認または失効を確認する。
- 承認済みの場合は、参加したデバイスグループを同期するために必要な情報を返す。
- 依頼を作ったデバイスだけが取得できる。

### `POST /api/groups/:groupId/device-requests/:requestId/approve`

- 既存デバイスが参加依頼のQRコードまたはURLを読み取り、依頼元デバイスを自身のデバイスグループへ追加する。
- 読み取りとこの操作自体を承認とし、依頼元デバイスによる追加の確認は求めない。
- 承認済みまたは失効済みの依頼は再利用できない。

## デバイスグループ

### `POST /api/groups`

- 登録済みのデバイスを単独メンバーとするデバイスグループを作る。

### `GET /api/groups/:groupId/state`

- 有効なメンバー一覧、保持中の鍵、そのデバイス向けの暗号化鍵パッケージ、および参加中のセッションを返す。
- 鍵にはサーバが付与したtimestamp、`recreated`、およびその鍵を共有するメンバー一覧を含める。

### `POST /api/groups/:groupId/keys`

- 有効なデバイスが、新しいグループ鍵と各メンバー向けの暗号化鍵パッケージを登録する。
- サーバは一意なtimestampを付与し、鍵のメンバー一覧と鍵パッケージの宛先が登録時点の有効メンバー一覧と完全に一致する場合だけ受理する。

### `DELETE /api/groups/:groupId/devices/:deviceId`

- 有効なデバイスが、自身または同じグループの別デバイスを有効メンバー一覧から外す。

## セッション

### `POST /api/sessions`

- 作成元プロセスを管理主体とする短命なセッションを作る。
- 最初のデバイスグループが参加するためのワンショット鍵交換も同時に用意する。
- セッションは作成元の活動から一日で失効する。

### `POST /api/sessions/:sessionId/pairings`

- 作成元プロセスが、別のデバイスグループ用のワンショット鍵交換を追加する。

### `POST /api/sessions/:sessionId/join`

- デバイスグループがワンショット鍵交換を使ってセッションへ参加する。
- セッション作成元による承認待ち状態は作らず、鍵交換の成立をもって参加とする。
- セッションとデバイスグループの関係は、以後のグループ鍵更新から独立して維持する。

### `GET /api/sessions/:sessionId/joins`

- 作成元プロセスが、参加済みデバイスグループと暗号化に利用可能な最新の公開鍵を取得する。

### `POST /api/sessions/:sessionId/events`

- 作成元プロセスが暗号化済みのイベントを保存する。
- 利用したグループ鍵が現在のメンバー一覧に対して有効でない場合は受理しない。
- 受理したイベントはセッションの失効時刻を延長する。

### `GET /api/sessions/:sessionId/events`

- 参加デバイスが、自身のデバイスグループ宛ての暗号化済みイベントを取得する。
- イベント取得はセッションの失効時刻を延長しない。

### `POST /api/sessions/:sessionId/responses`

- 参加デバイスが暗号化済みの選択結果または任意メッセージを保存する。
- 受理した応答は集約、選択または回答済み判定を行わず、セッションの失効時刻も延長しない。

### `GET /api/sessions/:sessionId/responses`

- 作成元プロセスが、デバイスから受理したすべての暗号化済み選択結果および任意メッセージを取得する。
- どの応答を採用するかは判断しない。

### `DELETE /api/sessions/:sessionId`

- 作成元プロセスがセッション全体を明示的に閉じる。
- セッションのイベント、応答、参加関係、および通知配送待ちを一括して失効させる。
